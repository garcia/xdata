#define XNAME pixel
#define XGROUP 1
#include "xstruct.h"
X(int, x)           GROUP(pix_pos)
X(int, y)           GROUP(pix_pos)
X(color *, color)   GROUP(pix_other)
X(float, alpha)     GROUP(pix_other)
